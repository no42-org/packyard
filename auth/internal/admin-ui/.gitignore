# Vite build output — generated by `make admin-ui`, embedded into the Go
# binary at compile time. Not checked in so feature branches don't churn
# binary diffs every time the SPA is rebuilt.
dist/
node_modules/
*.log
.vite/
